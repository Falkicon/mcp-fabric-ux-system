# Example environment variables for mcp-server-boilerplate

# Transport type: 'stdio' or 'http'
# MCP_TRANSPORT=stdio

# Port for HTTP/SSE transport (only used if MCP_TRANSPORT=http)
# MCP_PORT=3001

# Optional: Log level (e.g., 'debug', 'info', 'warn', 'error')
MCP_LOG_LEVEL=info
NODE_ENV=development

# Optional: Custom prefix for the greet tool
# CUSTOM_GREETING_PREFIX= 

# --- RAG Configuration ---
# Path to source documentation files (relative to project root)
DOCS_PATH=_docs_fabric_ux

# Pinecone Configuration (Required for indexDocs.ts and server)
PINECONE_API_KEY=YOUR_PINECONE_API_KEY
PINECONE_ENVIRONMENT=YOUR_PINECONE_ENVIRONMENT
# Example: "gcp-starter" or "us-west1-gcp" - Find in your Pinecone console
PINECONE_INDEX_NAME=fabric-ux-system
# Name of the index to create/use in Pinecone

# Embedding Model (from Hugging Face Hub via @xenova/transformers)
EMBEDDING_MODEL_NAME=Xenova/all-MiniLM-L6-v2

# --- MCP Server Authentication ---
# A secret key the server expects clients to send in the X-API-Key header
MCP_API_KEY=YOUR_SECURE_RANDOM_API_KEY

# Deprecated / Not used with hosted DB
# CHROMA_SERVER_URL=http://127.0.0.1:8000
# CHROMA_COLLECTION_NAME=fabric_ux_docs


pcsk_2amp1K_7ZQ6J6rAsxkiggJeapekTa5XCHxt5n2Cq4RYfq8dZ4o95kzReh8PjkppreuwsEU